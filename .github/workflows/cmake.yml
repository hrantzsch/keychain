name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run cmake
      run: cmake .
    - name: Build and run tests
      run: make test
